# Stopwatch

Tap the watch (or press Space / Enter while focused) to start, pause and resume. Reset stops and clears it. Elapsed time uses the monotonic performance clock rather than counting animation frames. Reduced motion changes the sweep to one-second steps.

Test: http://localhost:8080/plugin-tester.html?plugin=stopwatch
