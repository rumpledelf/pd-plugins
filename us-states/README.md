# US States Plugin

Scaffold copied from `sample/`.

This folder is reserved for a U.S. states map plugin with individually
addressable states, per-state colors, hover/tap tooltip behavior, and links to
state pages.
