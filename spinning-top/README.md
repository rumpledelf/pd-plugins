# Spinning top

Hold the playfield (or Space/Enter) to charge; release to spin. More charge means
more time spinning. The top slows, precesses, tips and settles. Releasing a
cancelled pointer or leaving the page cancels winding without launching.

Test at `plugin-tester.html?plugin=spinning-top`.
